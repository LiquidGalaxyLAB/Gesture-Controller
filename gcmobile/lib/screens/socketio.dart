import 'package:flutter/material.dart';

class SocketIoScreen extends StatelessWidget {
  Widget build(BuildContext context){
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Socket.io test screen'),
        )
      )
    );
  }
}
